import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build without error', (WidgetTester tester) async {
    // Placeholder test — Firebase harus di-mock sebelum testing widget penuh.
    expect(true, isTrue);
  });
}
